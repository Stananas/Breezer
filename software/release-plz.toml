# release-plz configuration.
# GitHub-only releases: versions come from git tags, never crates.io.
# (git_only skips `cargo publish` entirely.)

[workspace]
git_only = true
changelog_path = "CHANGELOG.md"
pr_labels = ["release"]